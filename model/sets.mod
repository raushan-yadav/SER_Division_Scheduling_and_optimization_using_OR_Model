# =========================
# Sets definition
# =========================

set T;          # Set of all trains
set Tf ⊆ T;     # Freight trains
set Tp ⊆ T;     # Passenger trains

set N;          # Nodes (stations / junctions)
set A;          # Track sections (arcs)

set As ⊆ A;     # Single-line sections
set J ⊂ N;   # junction nodes

